<html>
    <head>
        <link rel="stylesheet" href="/styles.css">
    </head>           
    <body>
        <h1><%= title %>
            <%- include('header', {}); %>
        </h1>
        <ul>
            <% messages.map((message) => { %>
                <%- include('message', {value: message}); %>
            <% }); %>
        </ul>
    </body>
</html>
